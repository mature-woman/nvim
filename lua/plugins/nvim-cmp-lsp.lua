-- A completion plugin for neovim coded in Lua.
return {
	'hrsh7th/cmp-nvim-lsp',
	enabled = true,
	lazy = true,
	dependencies = {
		-- A completion plugin for neovim coded in Lua.
		{ 'hrsh7th/nvim-cmp' }
	},
	opts = {
	},
	init = function()
	end
}
